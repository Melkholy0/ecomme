import { OnSalePIPPipe } from './on-sale-pip.pipe';

describe('OnSalePIPPipe', () => {
  it('create an instance', () => {
    const pipe = new OnSalePIPPipe();
    expect(pipe).toBeTruthy();
  });
});
